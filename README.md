    # Backend
     setup environment

    ```bash
    mkdir backend
    cd backend
    npm init -y
    npm install express mysql2 dotenv cors jsonwebtoken bcrypt
    npm install --save-dev nodemon
```
